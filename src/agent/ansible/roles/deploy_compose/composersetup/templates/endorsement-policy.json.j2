{
  "identities": [
{% for org in allpeerorgs %}
    {
      "role": {
        "name": "member",
        "mspId": "{{ org }}"
      }
    }{{ "," if not loop.last }}
{% endfor %}
  ],
  "policy": {
    "1-of": [
{% for i in range(allpeerorgs | length) %}
      {
        "signed-by": {{ i }}
      },
{% endfor %}
    ]
  }
}